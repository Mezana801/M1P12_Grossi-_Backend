package com.example.grossi.repository;

import com.example.grossi.model.UniteEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UniteEntityRepository extends JpaRepository<UniteEntity, Integer> {
}